from django.urls import path
from routes.views import *

urlpatterns = [
    #path('', home, name='home'),
    # path('detail/<int:pk>/', RoutesDetailView.as_view(), name='detail'),
    # path('add/', RoutesCreateView.as_view(), name='create'),
    # path('edit/<int:pk>/', RoutesUpdateView.as_view(), name='edit'),
    # path('delete/<int:pk>/', RoutesDeleteView.as_view(), name='delete'),
]